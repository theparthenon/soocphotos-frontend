import FaceClusterScatter from "@/components/Charts/FaceClusterGraph";

export default function FaceScatter(){
    return(
      <>
        <FaceClusterScatter height={window.innerHeight - 55} />
      </>
    )
  }
